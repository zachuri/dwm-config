#!/bin/sh

DMENU='dmenu -i -l 5'
choice=$(echo -e "logout\nsleep\nreboot\nshutdown\nquit-pmenu" | $DMENU)

case "$choice" in
	logout) pkill dwm ;;
	sleep) sudo systemctl hibernate ;;
	reboot) sudo systemctl poweroff ;;
	shutdown) sudo systemctl reboot ;;
	quit-pmenu) exit 0 ;;
esac
