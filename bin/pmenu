#!/bin/sh

DMENU='dmenu -i -l 5'
choice=$(echo -e "logout\nreboot\nshutdown\nquit-pmenu" | $DMENU)

case "$choice" in
	logout) pkill dwm ;;
	reboot) systemctl poweroff ;;
	shutdown) systemctl reboot ;;
	quit-pmenu) exit 0 ;;
esac
